'use strict';

describe('b2s.views.header module', function() {

    beforeEach(module('b2s.views.container'));

    describe('header controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('ContainerCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});