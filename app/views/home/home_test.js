'use strict';

describe('b2s.views.home module', function() {

    beforeEach(module('b2s.views.home'));

    describe('home controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var homeCtrl = $controller('HomeCtrl');
            expect(homeCtrl).toBeDefined();
        }));

    });
});